import React, { useEffect } from "react";
import api from "@api/api";

const AboutPage = () => {

    // TEST
    /*useEffect(() => {
        api.reject().then((res) => {
            console.info(res);
        }).catch(err => {
            console.info(err);
        })
    }, [])*/

    return <div>
        About Get The Tape
    </div>
};

export default AboutPage;